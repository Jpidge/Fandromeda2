FANDROMEDA TESTING HISTORY
==========================

STATUS UPDATE — 2026-09-19
---------------------------
The results below were generated before a confirmed feature over-lag was
fixed. The old pipeline excluded the target week correctly, but also excluded
the latest available prior-week game. Preserve these numbers as pre-correction
context only; do not use them as the benchmark for future feature decisions.
A corrected position-by-position 2025 holdout was completed after the fix.
The old aggregate rows remain pre-correction context only.

Purpose
-------
This file records model-validation results before new forecasting features
are added. It gives us a fair baseline, so changes are kept only when they
improve predictions on data the model did not train on.


2026-09-19 — First ML holdout validation
-----------------------------------------
Command:
    python gptindex.py --validate-ml

Test design:
    * Training seasons: 2018 through 2024
    * Untouched test season: 2025
    * Test examples: 17,376 player-week forecasts
    * Each historical projection uses only data available before the
      projected week. The target week's actual fantasy points are not used
      as an input feature.

Results:

    Model                                MAE   RMSE   Bias    Within 3   Within 5
    -----------------------------------  ----  -----  ------  ---------  ---------
    Baseline projection                  1.40   3.74  -0.20    85.2%      90.0%
    Machine Learned Weight Values        1.38   3.34  +0.05    84.7%      91.5%
    Machine Learned Points Projection    1.38   3.34  +0.06    84.0%      91.3%

Definitions:
    * MAE (mean absolute error): average distance between projected and
      actual fantasy points. Lower is better.
    * RMSE (root mean squared error): gives extra weight to larger misses.
      Lower is better.
    * Bias: average over- or under-projection. Near zero is better.
    * Within 3 / Within 5: percentage of projections within that many
      fantasy points of the actual result. Higher is better.

Interpretation:
    * Both ML approaches improved MAE slightly: 1.40 to 1.38.
    * Both ML approaches reduced RMSE from 3.74 to 3.34, about an 11%
      reduction in the effect of larger misses.
    * Both improved the within-5 rate. They trade a small reduction in
      within-3 rate for fewer larger errors.
    * The transparent learned-weight model and the more complex direct
      XGBoost model performed almost identically in this aggregate test.
      That is encouraging: the interpretable model currently earns its
      place without sacrificing measured accuracy.

Important limitation:
    These are aggregate results across player-weeks. Before relying on the
    result for every roster decision, test the same models separately for
    QB, RB, WR, and TE. An aggregate improvement can hide a weaker position
    group.

Dashboard correction made after this test:
    The live dashboard's plus/minus range was originally calculated around
    the baseline projection. When learned weights replaced the displayed
    projection, the range could remain centered on the old baseline. The
    range is now recalculated around the final displayed projection. This
    display correction does not change the holdout results above.

Next planned validation:
    1. Produce an offensive-only aggregate in addition to the position rows.
    2. Treat the corrected offensive and position results as the benchmark.
    3. Add one leakage-safe matchup feature group at a time.
    4. Re-run the same untouched-2025 test.
    5. Keep a new feature only when it improves relevant out-of-sample
       results without creating meaningful bias.


2026-09-19 — Corrected temporal holdout, by position
-----------------------------------------------------
Command:
    python index.py --validate-ml

Correction applied before this run:
    A Week W forecast now includes Week W-1 data and excludes Week W data.
    The prior validation used one-game-stale features and is retained above
    only as pre-correction context.

2025 holdout results (trained 2018–2024):

    Position  Best model                         Baseline MAE  Best MAE
    --------  ---------------------------------  ------------  --------
    QB        Machine Learned Points Projection       8.23       7.84
    RB        Machine Learned Weight Values            4.24       4.01
    WR        Machine Learned Points Projection        3.43       3.26
    TE        Machine Learned Points Projection        2.83       2.67

Notes:
    * ML improved MAE and RMSE for every listed offensive position.
    * The learned-weight model is still the primary Proj. Pts. forecast. It
      is transparent, is best for RB, and is nearly tied with direct ML in
      every other group.
    * Do not select a different production model per position from one season
      alone. Confirm the pattern with additional temporal folds first.
    * The original "All" row contained 17,376 records, while QB/RB/WR/TE
      contained 5,708. The remainder includes positions without dedicated
      offensive models and can artificially lower aggregate error. The
      validator now reports an explicit "Offense (QB/RB/WR/TE)" aggregate as
      the primary overall score, while retaining "All player records" as a
      diagnostic row.


2026-09-19 — Train/serve-consistent holdout benchmark
------------------------------------------------------
Command:
    python index.py --validate-ml

Pipeline condition:
    Training and live forecasting both include each player's final three
    prior-season games and available snap usage as carry-over context. The
    training run downloaded 2017 data so the 2018 training season receives
    the same carry-over treatment.

2025 holdout results (trained 2018–2024):

    Group                  Best model                         Baseline MAE  Best MAE
    ---------------------  ---------------------------------  ------------  --------
    Offense (QB/RB/WR/TE)  Machine Learned Points Projection       4.02       3.84
    QB                     Machine Learned Points Projection       8.32       7.94
    RB                     Machine Learned Weight Values            4.22       4.00
    WR                     Machine Learned Points Projection        3.38       3.23
    TE                     Machine Learned Points Projection        2.80       2.68

Key offense-wide results:

    Model                                MAE   RMSE   Bias    Within 3   Within 5
    -----------------------------------  ----  -----  ------  ---------  ---------
    Baseline projection                  4.02   6.06  +0.03    55.7%      71.1%
    Machine Learned Weight Values        3.87   5.61  +0.10    56.3%      74.4%
    Machine Learned Points Projection    3.84   5.60  +0.09    56.4%      74.4%

Interpretation:
    * Both ML layers improve the meaningful offensive aggregate over the
      baseline. Direct ML has a small MAE advantage, while learned weights
      remain nearly tied and are more transparent.
    * This single holdout does not justify selecting a different production
      model by position. Confirm any such policy with additional time-based
      folds first.
    * These values are the current benchmark for future feature work. The
      saved live ML artifacts must be retrained before use because the
      feature pipeline changed.
