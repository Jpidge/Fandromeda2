FANDROMEDA TESTING HISTORY
==========================

2026-09-19 — Canonical scoring engine, phase 1
------------------------------------------------
Implementation status: verified locally with
``python -m unittest discover -s tests -v``.

The authoritative Fantasy Football Coalition rules now live in
``fandromeda/scoring.py`` rather than being duplicated inside the application.
The module contains exact formulas for core non-PPR offense, long-touchdown
bonuses, return scoring, offensive fumble-return touchdowns, kicker scoring,
and D/ST scoring including all points-allowed tiers. The existing weekly
offensive-stat pipeline now calls this shared engine.

Test command:
    python -m unittest discover -s tests -v

Phase-2 limitation:
    Weekly player totals cannot identify long touchdowns, return events, or
    all K/DST events. Historical nflverse play-by-play will be loaded and
    aggregated before those event-specific fields are allowed into actuals,
    backtests, or projections.

2026-09-19 — Play-by-play scoring reconstruction, phase 2a
-----------------------------------------------------------
Implementation status: added; local test execution pending.

``fandromeda/pbp_scoring.py`` reconstructs a compact player-week event table
from nflverse play-by-play. It covers 40+ passing/rushing/receiving touchdown
bonuses, return yards/touchdowns, offensive fumble-return touchdowns, PATs,
and made field-goal distance. It rejects data without season/week and excludes
postseason plays. It does not yet alter production scoring: first inspect a
single cached season, then validate schema/event totals before integrating
those fields into historical actuals and retraining.

First controlled command:
    python index.py --build-scoring-events --season 2025

2026-09-19 — Play-by-play scoring reconstruction, phase 2b
-----------------------------------------------------------
Implementation status: added; local test execution pending.

Validated cached player-week event tables are now joined to weekly NFLverse
player statistics before the canonical scoring engine calculates actual
points. This updates scoring for available 40+ touchdown bonuses and return
events without inventing unavailable offensive fumble-return touchdowns.

Model implication:
    The historical targets have changed. Existing learned-weight and direct
    ML model files must be retrained, and the 2025 holdout must be rerun,
    before any new projection claims are made.

Artifact safety:
    Saved ML artifacts now record the scoring version. Models without the
    current ``ffc_yahoo_v1`` marker are deliberately not applied, preventing
    old target definitions from silently powering a newly scored dashboard.

Runtime improvement:
    Running ``--train-ml --learn-weights`` now builds the shared historical
    training table once, then fits both model layers from that same immutable
    in-memory table. It changes runtime, not the examples or model formulas.

2026-09-19 — PBP-adjusted scoring holdout and model refresh
------------------------------------------------------------
Command:
    python index.py --validate-ml
    python index.py --train-ml --learn-weights --week 2

Pipeline condition:
    Available player-level play-by-play events are included in historical
    actuals before feature creation and model fitting. The rare offensive
    fumble-return touchdown remains unavailable in the source schema and is
    not fabricated.

2025 holdout results (trained 2018–2024):

    Model                                MAE   RMSE   Bias    Within 3   Within 5
    -----------------------------------  ----  -----  ------  ---------  ---------
    Baseline projection                  4.19   6.19  +0.03     54.0%      70.2%
    Machine Learned Weight Values        4.00   5.74  +0.10     54.4%      73.7%
    Machine Learned Points Projection    3.97   5.73  +0.04     54.8%      73.6%

Interpretation:
    * This is a changed scoring target, not a direct apples-to-apples
      regression against the former core-offense benchmark.
    * Both ML models retain a measured advantage over the newly scored
      baseline. Direct ML has the narrowest MAE, while learned weights remain
      close and are the transparent primary projection.
    * Both live model artifacts were retrained after validation on 135,913
      historical player-weeks and now carry the ``ffc_yahoo_v1`` marker.

STATUS UPDATE — 2026-09-19
---------------------------
The results below were generated before a confirmed feature over-lag was
fixed. The old pipeline excluded the target week correctly, but also excluded
the latest available prior-week game. Preserve these numbers as pre-correction
context only; do not use them as the benchmark for future feature decisions.
A corrected position-by-position 2025 holdout was completed after the fix.
The old aggregate rows remain pre-correction context only.

Purpose
-------
This file records model-validation results before new forecasting features
are added. It gives us a fair baseline, so changes are kept only when they
improve predictions on data the model did not train on.


2026-09-19 — First ML holdout validation
-----------------------------------------
Command:
    python gptindex.py --validate-ml

Test design:
    * Training seasons: 2018 through 2024
    * Untouched test season: 2025
    * Test examples: 17,376 player-week forecasts
    * Each historical projection uses only data available before the
      projected week. The target week's actual fantasy points are not used
      as an input feature.

Results:

    Model                                MAE   RMSE   Bias    Within 3   Within 5
    -----------------------------------  ----  -----  ------  ---------  ---------
    Baseline projection                  1.40   3.74  -0.20    85.2%      90.0%
    Machine Learned Weight Values        1.38   3.34  +0.05    84.7%      91.5%
    Machine Learned Points Projection    1.38   3.34  +0.06    84.0%      91.3%

Definitions:
    * MAE (mean absolute error): average distance between projected and
      actual fantasy points. Lower is better.
    * RMSE (root mean squared error): gives extra weight to larger misses.
      Lower is better.
    * Bias: average over- or under-projection. Near zero is better.
    * Within 3 / Within 5: percentage of projections within that many
      fantasy points of the actual result. Higher is better.

Interpretation:
    * Both ML approaches improved MAE slightly: 1.40 to 1.38.
    * Both ML approaches reduced RMSE from 3.74 to 3.34, about an 11%
      reduction in the effect of larger misses.
    * Both improved the within-5 rate. They trade a small reduction in
      within-3 rate for fewer larger errors.
    * The transparent learned-weight model and the more complex direct
      XGBoost model performed almost identically in this aggregate test.
      That is encouraging: the interpretable model currently earns its
      place without sacrificing measured accuracy.

Important limitation:
    These are aggregate results across player-weeks. Before relying on the
    result for every roster decision, test the same models separately for
    QB, RB, WR, and TE. An aggregate improvement can hide a weaker position
    group.

Dashboard correction made after this test:
    The live dashboard's plus/minus range was originally calculated around
    the baseline projection. When learned weights replaced the displayed
    projection, the range could remain centered on the old baseline. The
    range is now recalculated around the final displayed projection. This
    display correction does not change the holdout results above.

Next planned validation:
    1. Produce an offensive-only aggregate in addition to the position rows.
    2. Treat the corrected offensive and position results as the benchmark.
    3. Add one leakage-safe matchup feature group at a time.
    4. Re-run the same untouched-2025 test.
    5. Keep a new feature only when it improves relevant out-of-sample
       results without creating meaningful bias.


2026-09-19 — Corrected temporal holdout, by position
-----------------------------------------------------
Command:
    python index.py --validate-ml

Correction applied before this run:
    A Week W forecast now includes Week W-1 data and excludes Week W data.
    The prior validation used one-game-stale features and is retained above
    only as pre-correction context.

2025 holdout results (trained 2018–2024):

    Position  Best model                         Baseline MAE  Best MAE
    --------  ---------------------------------  ------------  --------
    QB        Machine Learned Points Projection       8.23       7.84
    RB        Machine Learned Weight Values            4.24       4.01
    WR        Machine Learned Points Projection        3.43       3.26
    TE        Machine Learned Points Projection        2.83       2.67

Notes:
    * ML improved MAE and RMSE for every listed offensive position.
    * The learned-weight model is still the primary Proj. Pts. forecast. It
      is transparent, is best for RB, and is nearly tied with direct ML in
      every other group.
    * Do not select a different production model per position from one season
      alone. Confirm the pattern with additional temporal folds first.
    * The original "All" row contained 17,376 records, while QB/RB/WR/TE
      contained 5,708. The remainder includes positions without dedicated
      offensive models and can artificially lower aggregate error. The
      validator now reports an explicit "Offense (QB/RB/WR/TE)" aggregate as
      the primary overall score, while retaining "All player records" as a
      diagnostic row.


2026-09-19 — Train/serve-consistent holdout benchmark
------------------------------------------------------
Command:
    python index.py --validate-ml

Pipeline condition:
    Training and live forecasting both include each player's final three
    prior-season games and available snap usage as carry-over context. The
    training run downloaded 2017 data so the 2018 training season receives
    the same carry-over treatment.

2025 holdout results (trained 2018–2024):

    Group                  Best model                         Baseline MAE  Best MAE
    ---------------------  ---------------------------------  ------------  --------
    Offense (QB/RB/WR/TE)  Machine Learned Points Projection       4.02       3.84
    QB                     Machine Learned Points Projection       8.32       7.94
    RB                     Machine Learned Weight Values            4.22       4.00
    WR                     Machine Learned Points Projection        3.38       3.23
    TE                     Machine Learned Points Projection        2.80       2.68

Key offense-wide results:

    Model                                MAE   RMSE   Bias    Within 3   Within 5
    -----------------------------------  ----  -----  ------  ---------  ---------
    Baseline projection                  4.02   6.06  +0.03    55.7%      71.1%
    Machine Learned Weight Values        3.87   5.61  +0.10    56.3%      74.4%
    Machine Learned Points Projection    3.84   5.60  +0.09    56.4%      74.4%

Interpretation:
    * Both ML layers improve the meaningful offensive aggregate over the
      baseline. Direct ML has a small MAE advantage, while learned weights
      remain nearly tied and are more transparent.
    * This single holdout does not justify selecting a different production
      model by position. Confirm any such policy with additional time-based
      folds first.
    * These values are the current benchmark for future feature work. The
      saved live ML artifacts must be retrained before use because the
      feature pipeline changed.


2026-09-20 — Prediction snapshot store regression check
-------------------------------------------------------
Purpose:
    Confirm that each published forecast is retained as a distinct,
    append-only pre-evaluation record rather than being overwritten by the
    live projections CSV.

Commands:
    python -m unittest discover -s tests -p "test_snapshots.py" -v
    python index.py --no-open-dashboard
    python index.py --list-snapshots

Expected result:
    * The unit tests pass.
    * The normal run prints one new Parquet snapshot path and the CSV index.
    * The index lists the new capture timestamp, target week, row count, and
      primary projection model.
    * A second normal run creates another snapshot rather than replacing the
      first. Use `--no-snapshot` only for deliberate diagnostic reruns.

Result:
    PASS. All three snapshot regression tests completed successfully. The
    first live snapshot stored 2,176 player rows in a 108 KB Parquet file and
    added a readable index row identifying the learned-weight model as the
    primary projection. The immutable first record correctly retains the app
    version that the source reported at capture time; the stale source label
    was then corrected from v0.4.0-beta to v0.5.0-beta for future runs.


2026-09-21 — League-roster snapshot correction and Yahoo sample
-------------------------------------------------------------
Root cause:
    The snapshot writer received the entire NFL forecast pool, rather than
    the league roster. The latest exports contain 2,221 NFL projections but
    184 roster entries: 171 individual players and 13 D/ST across 12 managers.

Change:
    Normal runs now save roster-scoped schema-v2 snapshots with manager,
    slot, team-defense identity, and explicit missing-forecast status. Legacy
    v1 snapshots and their index rows are preserved. Waiver/live exports and
    the automated roster-refresh scripts were not changed.

Validation:
    PASS: python -B -m unittest discover -s tests -v — 25 tests, including
    six snapshot tests. Executed with the bundled Python 3.12 interpreter
    plus isolated workspace dependencies (nflreadpy, pyarrow, rapidfuzz).
    PASS: real-export temporary Parquet roundtrip retained 184 entries,
    12 managers, 169 forecasts and 15 explicit unavailable forecasts.
    Missing individual forecasts: Jordyn Tyson and Marvin Harrison Jr.;
    the remaining 13 are D/ST. Missing is not converted to zero.
    PASS: existing snapshot/manifest hashes unchanged across the smoke test.
    PASS: --list-snapshots displays legacy schema versions and scope note.
    No fresh production forecast was generated or old snapshot rewritten.

Yahoo follow-up:
    Saved an actual Week 3 matchup clipboard sample after expanding bench
    players: 31 entries from Purple Reign and Belichick Yo'self, including IR.
    Sample and parser requirements are in data/yahoo_projection_exports/.
    The capture validator accepts it. Numeric parsing, whole-league capture,
    identity reconciliation and kickoff-safe benchmarking remain pending.


2026-09-21 — Defensive matchup feature candidate holdout
----------------------------------------------------------
Command:
    python index.py --validate-ml --no-open-dashboard --no-snapshot

Experiment:
    Walk-forward training and 2025 holdout evaluation with a candidate
    feature for prior-week fantasy points allowed by opponent and position.
    The feature is isolated to validation and is not enabled in live
    production forecasts or saved live model artifacts.

Results (2025 holdout, offense QB/RB/WR/TE):

    Model                                MAE         RMSE        Bias
    -----------------------------------  ----------  ----------  ---------
    Baseline projection                  4.190000    6.190000    +0.030000
    Machine Learned Weight Values        3.997854    5.740614    +0.096499
    Machine Learned Points Projection    3.970540    5.728705    +0.050300

Comparison with the locked pre-feature benchmark:
    * Learned weights improved by approximately 0.0008 MAE, effectively
      neutral at this sample size.
    * Direct ML worsened by approximately 0.0010 MAE.
    * The candidate does not show a reliable improvement on this single
      temporal holdout, so it remains research-only.

Next decision:
    Keep the matchup feature behind the validation flag. Require another
    time-based fold and/or a meaningful improvement before retraining and
    promoting production models.


2026-09-21 — Defensive matchup candidate second temporal fold
---------------------------------------------------------------
Command:
    python index.py --validate-ml --season 2025 --no-open-dashboard --no-snapshot

Holdout:
    Train 2017–2023; test 2024.

Results (offense QB/RB/WR/TE):
    Baseline projection MAE: 4.15
    Machine Learned Weight Values MAE: 3.99
    Machine Learned Points Projection MAE: 3.98

Interpretation:
    ML improvement over the baseline is consistent with the 2025 fold. The
    defensive matchup candidate itself is still not isolated as a meaningful
    incremental improvement, so it remains validation-only.
2026-09-21 — Archived validation review workflow
------------------------------------------------
Both completed folds reproduced the earlier console metrics. Preserved the
full multi-fold transcript, full-precision 2025 CSV, and rounded 2024 offensive
metrics in data/output/validation_runs. The 2024 full-precision report had been
overwritten by the old multi-fold loop and cannot be recovered from that CSV.
New folds now receive separate immutable report filenames and fold metadata.
--validation-report exits before the live data and training pipeline.
Project Lab reads archived results. No historical model training was rerun.
Validation: 44 tests passed using bundled Python with existing test-deps;
report-only CLI smoke test passed; gptindex.py syntax parsed successfully.
Interpretation correction: the 2024 candidate versus heuristic comparison
does not isolate the matchup feature effect; matched feature-off ML results
would be needed. No production promotion is justified by this comparison.
User team: Purple Reign. The roster file contains all league teams; its first
team, Vader's Raiders, is not evidence of the user's team identity.
2026-09-21 — Release review correction (supersedes matchup conclusions)
---------------------------------------------------------------------
Review found that attach_defensive_history joined the offensive player's own
team to defensive history. Walk-forward features also retained the last game's
opponent, and history preferred generic fantasy_points over league scoring.
The original 2024/2025 metrics are preserved but cannot establish the benefit
or neutrality of opponent matchup features. Project Lab and report-only output
now label legacy results invalid for that purpose without rewriting archives.
Corrected research path resolves the target-week opponent from schedule team
identity, sets the feature season explicitly, joins opponent defense, and
prefers fantasy_points_std. No saved production models were retrained.
Validation: 46 short tests pass, including an integration test separating own
team, stale opponent, target opponent, prior-season identity, and target points.
No full historical rerun requested or performed. Before a future research run,
implement paired feature-off/on evaluation with shared feature construction.
Release remains on hold pending that workflow review and final artifact review.
Purple Reign (Yahoo team ID 10) confirmed via signed-in My Team link. Week 3
live roster shows Corum at flex and Nacua Out on bench; applied those changes
to the saved roster after backup. Other managers' saved rows were preserved.
